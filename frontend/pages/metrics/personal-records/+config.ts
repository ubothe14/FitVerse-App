import type { Config } from 'vike/types';

export default {
  prerender: true,
  title: 'Personal Records (PRs) — Gold, Silver &amp; Premature PRs | FitVerse',
  description:
    'Personal records (PRs) in FitVerse: what counts as a PR, the three PR tiers (Gold, Silver, Premature), and how to interpret PR droughts and frequency.',
} satisfies Config;
