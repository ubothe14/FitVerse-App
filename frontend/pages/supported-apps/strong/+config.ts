import type { Config } from 'vike/types';

export default {
  prerender: true,
  title: 'Strong CSV Export — Turn Your Workouts Into Free Analytics | FitVerse',
  description:
    'Export your Strong workout data as CSV and import into FitVerse. Get muscle heatmaps, plateau detection, PR tracking, and set-by-set coaching feedback — free, no account needed.',
} satisfies Config;
