import type { Config } from 'vike/types';

export default {
  prerender: true,
  title: 'Lyfta API — Import Workouts into FitVerse for Free Analytics',
  description:
    'Connect your Lyfta account via API key and get muscle heatmaps, plateau detection, PR tracking, and set-by-set feedback in FitVerse. Free, browser-first, runs in your browser.',
} satisfies Config;
